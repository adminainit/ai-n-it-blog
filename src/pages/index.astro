---
import { getCollection } from 'astro:content';
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero';
import PostCard from '../components/PostCard.astro';
import { siteConfig } from '../../site.config';
const baseUrl = import.meta.env.BASE_URL.endsWith('/') ? import.meta.env.BASE_URL : import.meta.env.BASE_URL + '/';

const allPosts = await getCollection('posts', ({ data }) => {
  return import.meta.env.PROD ? data.draft !== true : true;
});
const sortedPosts = allPosts.sort(
  (a, b) => b.data.date.valueOf() - a.data.date.valueOf()
);
const leadPost = sortedPosts[0];
const supportingPosts = sortedPosts.slice(1, 3);
const recentPosts = sortedPosts.slice(3, 6);
---

<Layout title={siteConfig.title} description={siteConfig.description}>
  <div class="mx-auto flex max-w-[1380px] flex-col gap-20 px-4 py-6 sm:px-6 md:py-10 lg:px-10">
    <Hero postCount={sortedPosts.length} client:load />

    {leadPost && (
      <section id="latest-insights" class="scroll-mt-24">
        <header class="mb-8 grid gap-5 border-b border-slate-300/80 pb-7 lg:grid-cols-[minmax(0,1fr)_minmax(18rem,32rem)] lg:items-end lg:gap-10 dark:border-slate-700">
          <div class="max-w-3xl">
            <p class="type-eyebrow mb-3 font-mono font-semibold uppercase tracking-[0.18em] text-[var(--site-secondary)]">{siteConfig.homePage?.latestEyebrow || 'The intelligence desk'}</p>
            <h2 class="type-section-title text-balance font-display font-semibold tracking-[-0.025em] text-slate-950 dark:text-white">{siteConfig.homePage?.latestTitle || 'Latest perspectives'}</h2>
          </div>
          <div class="flex flex-col items-start gap-4 lg:items-end lg:text-right">
            <p class="type-body max-w-xl text-slate-600 dark:text-slate-300">{siteConfig.homePage?.latestDescription || 'Every current article, visible at a glance.'}</p>
            <a href={baseUrl + 'blog'} class="type-button inline-flex items-center gap-2 font-extrabold uppercase tracking-[0.12em] text-slate-900 transition hover:text-teal-700 dark:text-white dark:hover:text-teal-300">
              {siteConfig.homePage?.latestLinkText || 'View archive'} <span aria-hidden="true">↗</span>
            </a>
          </div>
        </header>

        <div class="grid gap-6 lg:grid-cols-[1.32fr_.68fr]">
          <PostCard post={leadPost} featured={true} index={0} />
          {supportingPosts.length > 0 && (
            <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-1">
              {supportingPosts.map((post, index) => (
                <PostCard post={post} index={index + 1} />
              ))}
            </div>
          )}
        </div>
      </section>
    )}
  </div>

  {recentPosts.length > 0 && (
    <section class="mx-auto flex max-w-[1380px] flex-col gap-6 px-4 pb-20 sm:px-6 lg:px-10">
      <div class="flex flex-col gap-4 border-b border-slate-300/80 pb-6 sm:flex-row sm:items-end sm:justify-between dark:border-slate-700">
        <div class="max-w-3xl">
          <p class="type-eyebrow mb-2 font-mono font-semibold uppercase tracking-[0.18em] text-[var(--site-accent)]">{siteConfig.homePage?.archiveEyebrow || 'From the archive'}</p>
          <h2 class="type-section-title text-balance font-display font-semibold text-slate-950 dark:text-white">{siteConfig.homePage?.archiveTitle || 'More to explore'}</h2>
        </div>
        <a href={baseUrl + 'blog'} class="type-button font-extrabold uppercase tracking-[0.12em] text-slate-900 hover:text-[var(--site-secondary)] dark:text-white">{siteConfig.homePage?.archiveLinkText || 'View all'}</a>
      </div>

      <div class="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
        {recentPosts.map((post, index) => (
          <PostCard post={post} index={index + 3} />
        ))}
      </div>
    </section>
  )}
</Layout>
